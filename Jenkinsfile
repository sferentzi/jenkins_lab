pipeline {
    agent any
    stages {
        stage('Devops From github') {
            steps {
                sh 'echo "Ping google.com"'
                sh 'ping google.com -c 5'
            }
        }
    }
}
